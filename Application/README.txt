Để chạy sử dụng WebApp cần chạy file DiabetesPrediction.py trước để tạo model và lưu model vào các file model.pkl

Cách chạy Web_App(streamlit):
- Cài thư viện streamlit: `pip install streamlit`
- cd đến folder chứa file WebApp.py
- chạy lệnh trên terminal: streamlit run WebApp.py